<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Login activity styles -->
    <style name="ParentStyle" parent="Widget.AppCompat.Button">
        <item name="android:padding">@dimen/_10dp</item>
        <item name="android:textSize">@dimen/_18sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="FacebookBtnStyle" parent="ParentStyle" >
        <item name="android:background">@drawable/bg_fb_btn</item>
        <item name="android:drawableStart">@drawable/ic_facebook</item>
    </style>

    <style name="GoogleBtnStyle" parent="ParentStyle" >
        <item name="android:background">@drawable/bg_google_btn</item>
        <item name="android:drawableStart">@drawable/ic_google</item>
    </style>


</resources>